---
title: RuntimeEnvironmentHandler
---

# RuntimeEnvironmentHandler
_namespace: [ShoalShell.Plugins.Runtime.Environment](N-ShoalShell.Plugins.Runtime.Environment.html)_

假若将本模块直接集成在Shoal的主程序之中，则在Windows 7或者更高版本的Windows之中，假若开启了UAC的话，则程序每一次执行都必须要管理员权限了，所以现在将涉及到系统底层的操作都放在一个模块之中以避免这个问题




